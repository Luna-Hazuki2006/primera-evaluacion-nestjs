import { ClienteActualizarDto } from './cliente_actualizar.dto';

describe('ClienteActualizarDto', () => {
  it('should be defined', () => {
    expect(new ClienteActualizarDto()).toBeDefined();
  });
});
